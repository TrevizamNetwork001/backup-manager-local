# Arquivos protegidos da interface de login

Arquivos identificados:

- `backup_manager/app.py` — função `login_page` e HTML do formulário;
- `static/app.css` — layout, cores, card e responsividade;
- `static/assets/fundo.png` — background visual, quando presente.

Antes e depois de deploy, compare os hashes com o manifesto de integridade.
Nenhum instalador deve copiar templates genéricos sobre esses arquivos.
